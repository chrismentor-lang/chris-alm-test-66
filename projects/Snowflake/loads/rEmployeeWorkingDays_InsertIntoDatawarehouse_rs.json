{
  "-name": "rEmployeeWorkingDays_InsertIntoDatawarehouse_rs",
  "-type": "RelationalSQL",
  "comment": {
    "#cdata-section": "Calls procedure from accountability schema in the HARMONISATION database that captures the working days to build a forward-looking calendar of which days each active employee is scheduled to work in EMPLOYEE_WORKING_DAYS table in the DW schema of the DATAWAREHOUSE database."
  },
  "connection": {
    "-nameref": "csfJedoxIntegrator_Global_sc"
  },
  "query": {
    "#file": "rEmployeeWorkingDays_InsertIntoDatawarehouse_rs.query.sql"
  }
}
