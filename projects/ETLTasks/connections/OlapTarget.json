{
  "-name": "OlapTarget",
  "-type": "JedoxOlap",
  "comment": {
    "#cdata-section": "Reference to Jedox target connection defined in Connection Manager. Only Connections which don't use user credentials are allowed."
  },
  "database": "${TargetDB}",
  "global": {
    "-nameref": "${TargetConn}"
  }
}
